apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "magiclantern.fullname" . }}-test-connection"
  labels:
    {{- include "magiclantern.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "magiclantern.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
