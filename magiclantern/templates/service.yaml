apiVersion: v1
kind: Service
metadata:
  name: {{ include "magiclantern.fullname" . }}
  labels:
    {{- include "magiclantern.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "magiclantern.selectorLabels" . | nindent 4 }}
